<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.jankowski.rafal</groupId>
    <artifactId>job-assistant</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>job-assistant</name>
    <description>AI-assisted job offer analysis, skill-gap reporting and tailored CV generation</description>

    <properties>
        <java.version>21</java.version>
        <kotlin.version>2.3.21</kotlin.version>
        <spring-modulith.version>2.1.0</spring-modulith.version>
        <langchain4j.version>1.19.0</langchain4j.version>
        <playwright.version>1.62.0</playwright.version>

        <!-- Frontend build; only used by the `frontend` profile below. -->
        <frontend-maven-plugin.version>2.0.2</frontend-maven-plugin.version>
        <node.version>v24.19.0</node.version>

        <!-- Test tiers. The fast tier is the default; -P pdf / -P eval switch to the others.
             JUnit applies exclusions before inclusions, so these must be swapped, not added to. -->
        <test.groups/>
        <test.excludedGroups>eval,pdf</test.excludedGroups>

        <!-- Coverage; only used by the `coverage` profile below. -->
        <jacoco.version>0.8.13</jacoco.version>

        <!--
            SonarQube Cloud. Analysis is CI-based rather than Automatic, because `main` is
            merges-only: automatic analysis fully covers the default branch, which is the one place
            a finding arrives too late to act on. It also cannot import coverage at all.
        -->
        <sonar.host.url>https://sonarcloud.io</sonar.host.url>
        <sonar.organization>anielskieoczko</sonar.organization>
        <sonar.projectKey>AnielskieOczko_job-assistant</sonar.projectKey>
        <!-- The frontend is the larger half of the codebase (~7.7k lines of TS/TSX against ~5.5k
             of Kotlin), and the Maven scanner sees only Java source roots without this. -->
        <sonar.maven.scanAll>true</sonar.maven.scanAll>
        <sonar.exclusions>
            frontend/node_modules/**,frontend/dist/**,frontend/.mvn-node/**,src/main/resources/static/**,graphify-out/**,target/**
        </sonar.exclusions>
        <!--
            Two honest exclusions, neither of them a way to hide untested code:
            - the frontend has no test suite yet, so measuring it would report 0%, not a fact;
            - PlaywrightDocumentRenderer IS tested, by the `pdf` tier, which the coverage job
              deliberately does not run (it would mean downloading Chromium a second time).
            Delete the first line the day frontend tests exist.
        -->
        <sonar.coverage.exclusions>
            frontend/**,**/PlaywrightDocumentRenderer.kt
        </sonar.coverage.exclusions>
    </properties>

    <profiles>
        <profile>
            <!-- Renders through headless Chromium; downloads the browser on first run. -->
            <id>pdf</id>
            <properties>
                <test.groups>pdf</test.groups>
                <test.excludedGroups/>
            </properties>
        </profile>
        <profile>
            <!--
                JaCoCo, for the SonarQube Cloud job. Off by default on the same principle as the
                test tiers: a plain `./mvnw test` should not carry an agent it has no use for.
                `prepare-agent` instruments the fast tier, `report` writes the XML the scanner
                auto-detects at target/site/jacoco/jacoco.xml during `verify`.
            -->
            <id>coverage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>${jacoco.version}</version>
                        <executions>
                            <execution>
                                <id>prepare-agent</id>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>report</id>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                                <configuration>
                                    <formats>
                                        <format>XML</format>
                                    </formats>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <!-- Calls a live model against labelled fixtures. Costs tokens. -->
            <id>eval</id>
            <properties>
                <test.groups>eval</test.groups>
                <test.excludedGroups/>
            </properties>
        </profile>
        <profile>
            <!--
                Builds the Vite SPA into src/main/resources/static so `package` ships it inside
                the jar. Off by default on the same principle as the test tiers above: `./mvnw
                test` must never need npm, a network, or a 200 MB node_modules.

                Bound to generate-resources rather than prepare-package because Vite writes into
                src/main/resources, which maven-resources-plugin copies to target/classes during
                process-resources - the very next phase. Binding any later would package the
                previous build's assets. Executions inside one phase run in declaration order.
            -->
            <id>frontend</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.github.eirslett</groupId>
                        <artifactId>frontend-maven-plugin</artifactId>
                        <version>${frontend-maven-plugin.version}</version>
                        <configuration>
                            <workingDirectory>${project.basedir}/frontend</workingDirectory>
                            <installDirectory>${project.basedir}/frontend/.mvn-node</installDirectory>
                        </configuration>
                        <executions>
                            <execution>
                                <!-- A pinned Node, so the build does not depend on whatever nvm last selected. -->
                                <id>install-node-and-npm</id>
                                <goals>
                                    <goal>install-node-and-npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <nodeVersion>${node.version}</nodeVersion>
                                </configuration>
                            </execution>
                            <execution>
                                <id>npm-ci</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <arguments>ci</arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <!-- `tsc -b && vite build`, so this also type-checks the frontend. -->
                                <id>npm-build</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <arguments>run build</arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.modulith</groupId>
                <artifactId>spring-modulith-bom</artifactId>
                <version>${spring-modulith.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>dev.langchain4j</groupId>
                <artifactId>langchain4j-bom</artifactId>
                <version>${langchain4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Web / API -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Persistence -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Document rendering -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>com.microsoft.playwright</groupId>
            <artifactId>playwright</artifactId>
            <version>${playwright.version}</version>
        </dependency>

        <!-- Module boundaries -->
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-core</artifactId>
        </dependency>

        <!-- AI -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-open-ai</artifactId>
        </dependency>

        <!-- Kotlin -->
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-reflect</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib</artifactId>
        </dependency>
        <dependency>
            <groupId>tools.jackson.module</groupId>
            <artifactId>jackson-module-kotlin</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-test-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <sourceDirectory>${project.basedir}/src/main/kotlin</sourceDirectory>
        <testSourceDirectory>${project.basedir}/src/test/kotlin</testSourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <configuration>
                    <!-- LangChain4j reads @P-less tool/AI-service parameter names via reflection -->
                    <javaParameters>true</javaParameters>
                    <args>
                        <arg>-Xjsr305=strict</arg>
                        <arg>-Xannotation-default-target=param-property</arg>
                    </args>
                    <compilerPlugins>
                        <plugin>spring</plugin>
                    </compilerPlugins>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.jetbrains.kotlin</groupId>
                        <artifactId>kotlin-maven-allopen</artifactId>
                        <version>${kotlin.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <groups>${test.groups}</groups>
                    <excludedGroups>${test.excludedGroups}</excludedGroups>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
